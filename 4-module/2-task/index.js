function makeDiagonalRed(table) {
  for (i = 0; table.rows.lenght; i++) {
    console.log(table.rows[i]);
  }
  console.log(table.rows)
  Array.from(table.rows).forEach(element => {
    console.log(element)
  });
}
